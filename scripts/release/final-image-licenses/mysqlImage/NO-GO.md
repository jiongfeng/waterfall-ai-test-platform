# MySQL image license inventory not approved

Replace this marker with the complete reviewed license and notice material for
the exact MySQL image included in the offline bundle. Then bind the review to
the immutable image reference in `LICENSE-REVIEW.json`. Until that review is
complete, the offline release workflow must fail closed.
