# 安全政策

## 支持状态

本项目目前处于公开 Beta 阶段。安全修复面向最新受支持 Beta Release 和默认分支；
更早的历史标签、旧提交和第三方重打包版本不承诺获得安全更新。

| 版本或部署形态 | 安全支持 |
| --- | --- |
| 最新受支持 Beta Release，签名与附件验证通过 | 支持 |
| 默认分支最新提交，按受支持边界评估 | 尽力支持 |
| 更早的历史标签、旧提交和第三方重打包版本 | 不支持 |

## 受支持安全边界

当前公开 Beta 只支持以下边界：

- 由可信运维人员管理的 Linux/amd64 Docker 环境；
- 单一组织、单租户使用，账号由同一信任域管理；
- 平台、OpenCode、数据库和被测系统位于受控网络；
- 外部访问经过 TLS 反向代理、网络访问控制和独立强凭据；
- 测试数据、日志、报告、视频、诊断包和备份按敏感数据管理。

当前版本不是经过强化的公网 SaaS，也不提供不可信租户之间的隔离保证。以下场景不在支持范围内：

- 将 Flask 开发服务、OpenCode 或数据库端口直接暴露到公网；
- 允许任意互联网用户注册、登录或上传需求；
- 多租户、跨组织或相互不信任用户共享同一实例；
- Windows 或 macOS 原生生产部署；
- Kubernetes、高可用、多节点或零信任部署；
- 使用真实生产凭据、生产数据库或不可恢复的数据执行测试。

Beta 边界不意味着已知漏洞可以被忽略。即使部署在可信网络，也应遵循最小权限、
默认拒绝，并保护包含凭据的实际配置、数据库、工作区和备份。

## 私密报告漏洞

请优先使用 GitHub Private Vulnerability Reporting：

1. 打开本仓库的 **Security** 页面。
2. 选择 **Report a vulnerability**。
3. 在私密报告中提供受影响提交或镜像版本、攻击前提、复现步骤、影响、建议缓解措施和已脱敏证据。

不要在公开 Issue、Pull Request、Discussion、日志或截图中披露未修复漏洞、利用代码、凭据、Cookie、Token、内部地址或客户数据。

如果 GitHub 页面暂未显示私密报告入口，可以创建一个不含技术细节的公开 Issue，仅说明“需要私下报告安全问题”，请求维护者启用或提供 GitHub 私密沟通渠道。请勿把漏洞内容附在该 Issue 中。本项目不发布或编造未建立的安全邮箱。

## 响应目标

维护者将按可用资源尽力达到以下目标：

- 3 个工作日内确认收到报告；
- 7 个工作日内完成初步分级并说明下一步；
- 高风险问题优先准备修复、缓解措施和发布说明；
- 在修复可用并完成必要协调后再公开细节。

这些目标不是服务等级协议，也不构成奖励或赔偿承诺。

## 报告者准则

- 只测试你拥有或已获明确授权的实例和数据；
- 避免拒绝服务、批量数据访问、持久化控制和横向移动；
- 只收集证明问题所必需的最少数据，并安全删除；
- 给维护者合理修复时间，不以公开披露施压；
- 不访问真实第三方系统，不测试本仓库文档中出现的历史地址。

## 部署者责任

- 为平台会话、管理员、数据库、OpenCode 和被测系统分别配置独立强凭据；
- 会话和初始管理员秘密优先通过环境变量或 secret 管理提供；数据库、OpenCode 和
  被测系统凭据按兼容契约保存在未跟踪且权限受限的实际配置文件中；
- 被测系统凭据可能进入 Prompt、测试脚本、日志和工作区 Git，因此只能使用可撤销、
  最小权限的非生产账号；平台服务和生产凭据不得进入这些链路；
- 将模型提供方视为外部数据处理方，发送需求、页面内容或凭据前完成授权和合规评估；
- 限制容器文件系统、出站网络、挂载目录和数据库权限；
- 对诊断包和运行产物做人工复核；平台没有能够覆盖所有自由文本、视频、截图和
  trace 的全局自动脱敏；
- 定期备份并验证恢复，及时更新基础镜像和依赖。

一般使用问题请参阅 [SUPPORT.md](./SUPPORT.md)。
