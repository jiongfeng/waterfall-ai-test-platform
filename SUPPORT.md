# 支持政策

## 当前支持范围

本项目处于公开 Beta 准备阶段，当前没有公开安装制品。源码候选仅在可信环境、
单租户、Linux/amd64 Docker 边界内提供尽力社区支持；正式发行支持只有在对应
公开 Release 及其验证材料实际存在后才生效。

| 场景 | 状态 |
| --- | --- |
| 单一组织在受控网络中评估默认分支 Linux/amd64 Docker 源码候选 | 尽力协助 |
| 使用已验证的最新公开 Beta Release | 条件支持（该 Release 实际存在后） |
| Linux 上的本地开发和测试 | 尽力协助 |
| 公网直接暴露、不可信用户或多租户共享实例 | 不支持 |
| Windows/macOS 原生生产部署 | 不支持 |
| Kubernetes、高可用、多节点和自定义编排 | 不支持 |
| 历史标签、自定义基础镜像或第三方重打包版本 | 不支持 |
| 真实生产系统上的破坏性测试 | 不支持 |

“不支持”不表示相关场景永远不会实现，而是维护者目前不会承诺复现、兼容、迁移或安全保障。

## 获取帮助

### 使用和故障问题

在 GitHub Issue 中提交可复现的问题。创建 Issue 前请：

1. 确认使用默认分支最新代码或明确记录所用提交；
2. 搜索已有 Issue，避免重复；
3. 在隔离环境复现；
4. 对日志、配置、截图和诊断包完成脱敏。

Issue 建议包含：

- 提交 SHA 或镜像标识；
- Linux 发行版、Docker Engine 与 Compose 版本；
- 浏览器、Node.js、Python、MySQL 和 Playwright 版本；
- 最小复现步骤、预期结果和实际结果；
- 已执行的排查步骤；
- 最短且已脱敏的错误日志。

不要上传配置原件、数据库转储、完整测试报告、视频、trace、Cookie、Token、账号密码、个人路径或内部地址。

### 功能建议

使用功能建议模板说明待解决的问题、用户场景、与 Beta 边界的关系、替代方案及安全和运维影响。较大改动应在编码前等待方向确认。

### 安全问题

不要公开提交。请按 [SECURITY.md](./SECURITY.md) 使用 GitHub Private Vulnerability Reporting。

## 社区支持方式

维护者和贡献者会按可用时间提供帮助，但项目不提供响应时间、修复时间、兼容期限或商业服务等级承诺。问题处理优先级通常为：

1. 可利用的安全问题和数据损坏；
2. 受支持部署中的阻断性回归；
3. 可重复的功能缺陷；
4. 文档和可用性问题；
5. 功能建议及不受支持环境的问题。

需要生产级保障、定制部署或迁移服务的用户，应自行安排具备相应责任能力的工程与运维支持。

## 问题关闭

Issue 可能因无法复现、超出支持边界、长期无反馈、重复、缺少脱敏证据或已经修复而关闭。提供新证据后可以请求重新评估。
