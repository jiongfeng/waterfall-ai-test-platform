# 路线图

本路线图表达当前方向，不是交付日期或兼容性承诺。优先级会根据安全问题、用户反馈和维护资源调整。

## 项目定位

Waterfall AI 当前是面向可信团队的单租户 Linux Docker Beta，用于管理 Playwright 测试计划、脚本、Agent 任务、执行证据和本地 Git 版本。

在完成明确的安全与运维门槛前，项目不会宣称支持公网不可信用户或多租户 SaaS。

## 当前：公开 Beta 加固

### 安全基线

- 认证和授权采用默认拒绝，并对每个 API method 建立覆盖测试；
- 对 Markdown、HTML、归档、路径和命令输入建立一致的安全边界；
- 明确记录文件凭据兼容边界；被测系统账号只使用可撤销、最小权限的非生产凭据，
  并将模型、脚本、日志、产物和 Git 纳入其暴露范围；
- 为模型、浏览器自动化和诊断包建立数据暴露说明与最小权限默认值；
- 完成威胁模型、秘密扫描和发布前安全复核。

### 可重复发行

- 提供受支持的 Linux Docker 构建与部署路径；
- 固定依赖并验证离线安装、升级、备份和回滚；
- 为镜像和离线包提供校验和、SBOM 与第三方许可证；
- 从干净仓库生成发行物，避免本地配置和历史产物混入。

### 社区基础

- 建立许可证、安全报告、贡献、支持和治理规则；
- 使用 Issue 与 Pull Request 模板收集可复现信息；
- 自动跟踪 Python 和 npm 依赖更新；
- 维护变更日志和明确的支持矩阵。

## 下一阶段：可靠性与可观测性

- 后台任务重启恢复、幂等重试和取消一致性；
- 数据库迁移预检、备份验证和失败回滚；
- 项目导入导出的兼容版本与完整性验证；
- 结构化日志、任务指标和敏感字段过滤；
- 大型测试集、报告和产物的容量限制与保留策略；
- 面向 Linux Docker 的升级和灾难恢复演练。

## 稳定版门槛

稳定版候选至少需要：

- 完成独立安全审查并关闭阻断问题；
- 所有写操作都有明确授权、审计和负向测试；
- 支持的安装、升级、备份和恢复路径可重复验证；
- 数据格式、API 和迁移策略有稳定承诺；
- 发行物具备来源、校验和、SBOM 和许可证清单；
- 文档不包含环境专属凭据、地址、个人数据或内部业务资料；
- 有至少一个完整 Beta 周期的真实使用反馈。

## 暂不计划

以下能力不属于当前 Beta 路线：

- 公网开放注册或匿名使用；
- 不可信组织共享实例的多租户隔离；
- Windows 或 macOS 原生生产支持；
- Kubernetes、高可用、跨区域或多节点调度；
- 托管模型密钥、企业身份联合或商业 SLA；
- 任意第三方插件在宿主机上无隔离执行。

如需提议调整方向，请创建功能建议 Issue，说明用户问题、安全边界、运维成本和可维护方案。
